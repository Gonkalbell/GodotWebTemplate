# Export builds of the game don't need to be tracked by git.
build/

# This file appears to be autogenerated whenever the editor is open, so don't commit it in.
game/addons/godot-git-plugin/win64/~libgit_plugin.windows.editor.x86_64.dll
